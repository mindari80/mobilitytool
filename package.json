{
  "name": "dlt-gps-shp-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "python3 -m http.server 8080",
    "serve": "python3 -m http.server 8080",
    "test": "node --test tests/",
    "test:watch": "node --test --watch tests/"
  },
  "engines": {
    "node": ">=18"
  }
}
